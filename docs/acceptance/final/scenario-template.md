# Scenario: <A/B/C/D/E> — <not run/pass/fail/blocked>

* UTC start/end:
* Code revision and relevant local changes:
* Provider/model:
* Exact natural-language request:
* Sending wallet and recipient/resource:
* Discovery subject, verified domain and relation (or N/A for configured L402 URL):
* Requested asset ID/ticker/precision and amount:
* Plan ID; invoice payment hash, amount and expiry (no L402 credentials):
* Policy decision, actual limits, and reason:
* Approval required/result (automatic/human-approved/rejected):
* Payment hash and final authoritative node status (or no submission):
* Resource status/HTTP status for L402:
* Alice result and Bob result where applicable:
* Before/after snapshot paths and balance deltas (RGB, BTC, carrier/fees):
* Activity hash/direction/status:
* Count of new distinct outgoing payments:
* Submission audit evidence/count (or explicitly unavailable):
* Reservation-count comparison for reuse/rejection (sanitized; no journal dump):
* Screenshots and sanitized observations:
* Unexpected behavior, uncertainties, blockers:

Do not include OPENAI_API_KEY, authorization headers, CSRF tokens, macaroons,
preimages/proofs, node tokens, merchant keys, private keys or seeds.
