Human-recipient agent live acceptance — BLOCKED

No conversational agent session was started; this is a preflight transcript, not a payment transcript.

1. Existing non-reset regtest startup completed and reported both node APIs ready.
2. Subsequent scripts/regtest/status.sh showed running Alice, Bob, Bitcoin and proxy containers, then failed with connection refused.
3. Direct read-only GET /nodeinfo: Alice HTTP 403 LockedNode; Bob HTTP 403 LockedNode.
4. No deployed recipient identifier or public-HTTPS WebFinger/same-origin RGB invoice service was supplied or found in configuration.
5. No OpenAI requests, discovery/acquisition calls, prepared plans, approvals or payment submissions occurred. No current balances or settlement status were obtained.
6. Relevant offline recipient regression suite: 35 passed, 0 failed.

Resume prerequisites: restore indexer readiness and unlock existing funded nodes; provide a real recipient identifier backed by the production-compatible HTTPS service. Verify fresh balances and usable asset before running the agent. For a 5-unit human-approval demonstration, a process-only AUTO_APPROVE_BELOW=5 override is proposed but has not been applied. Any eventual approval must be collected through the application for its actual bound plan.

No architecture or security-policy changes were made. No credentials are included.
