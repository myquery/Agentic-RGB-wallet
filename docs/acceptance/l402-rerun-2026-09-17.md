# L402 live acceptance rerun — September 17, 2026

Passed through Alice's existing wallet API and real OpenAI agent. This was an API
acceptance run, not a new browser visual inspection. No application code changes
were required. The merchant was started and the idle Alice API restarted with
`config/l402.env.example`; existing wallet state was preserved.

- Request: `Get the premium report.`
- Resource: `http://127.0.0.1:3040/premium/report`.
- Price: 3 BTC Lightning sats, automatically approved under the inclusive 10-sat
  machine threshold (100-sat individual limit, 500-sat daily limit).
- Payment: `ed01e1dfed453ecf7cf784efddb2fa9bdd22e3f3e7cb4ebcf96395415a3d9eb2`.
- Alice and Bob independently returned `Succeeded`, 3,000 msat, no RGB asset.
- Authenticated resource result: `purchased`, HTTP 200, deterministic demo report.
- Channel local balance: Alice 77,013 → 77,010 sats; Bob 22,000 → 22,003 sats.
- One durable machine reservation. No replacement payment or payment retry.
- Above-threshold request: `Get the extended premium report at http://127.0.0.1:3040/premium/extended.`
  The 50-sat plan required human approval and was cancelled via its bound
  application rejection endpoint. No 50-sat reservation or payment was made.
- No provider error observed. Authentication secrets and payment proof omitted.

[Sanitized application results, policy, cancellation and independent node evidence](l402-rerun-2026-09-17.json).

The merchant and Alice API remain running. Repeated requests for the same resource
reuse the saved payment; they do not initiate a fresh purchase. To keep commerce
enabled after a future Alice API restart, source `config/l402.env.example` with
export enabled before launching `scripts/wallet/api.sh alice`.
